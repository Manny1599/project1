##takes in a list or frame of data
from make import *
def datamsg (frameD)
    msg = ['D', id, dest, new_object.sNum, frame]
    if frameD[0] != "D":
        print("error got a frame not meant to be data")
    if frameD[1] == frameD[2]:
        print("error sending message to itself")
    else:
        TranToNet = "D"
        count = len(frame)
        TranToNet.append(dest, count, )